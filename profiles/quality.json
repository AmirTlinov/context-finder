{
  "experts": {
    "schema_version": 1,
    "semantic": {
      "default": ["bge-small"],
      "identifier": ["bge-small"],
      "path": ["bge-small"],
      "conceptual": ["bge-base", "multilingual-e5-small"]
    },
    "graph_nodes": { "default": ["bge-small"] }
  }
}
